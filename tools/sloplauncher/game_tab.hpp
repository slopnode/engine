#pragma once

#include "launcher_state.hpp"

namespace sloplauncher {

/** "Game" tab: profile picker, --debug toggle, per-package cli.s7 options, and Launch. */
void drawGameTab(LauncherState& state);

}
