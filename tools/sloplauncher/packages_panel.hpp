#pragma once

#include "launcher_state.hpp"

namespace sloplauncher {

/** Left column: editable load-directory list on top, discovered package list below. */
void drawPackagesPanel(LauncherState& state);

}
