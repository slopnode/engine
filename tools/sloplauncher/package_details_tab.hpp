#pragma once

#include "launcher_state.hpp"

namespace sloplauncher {

/** "Package" tab: details for whichever package was last clicked in the list, incl. depends. */
void drawPackageDetailsTab(LauncherState& state);

}
