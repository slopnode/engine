#pragma once

#include "launcher_state.hpp"

namespace sloplauncher {

/** "Development" tab: pick a save target and launch slopmap/slopsprite/slopthing against it. */
void drawDevelopmentTab(LauncherState& state);

}
